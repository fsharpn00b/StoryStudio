// Starting script is always named start.txt.

// TODO1 #parsing window.state is hardcoded into the framework logic and must be defined. Maybe it should be hardcoded or given a default definition of {}?
// Set up initial state.
js
window.state = {
  player_state : {
    is_lost : false
  }
};
endjs

// Set up function.
js
window.is_lost = (function () {
  if (true === window.state.player_state.is_lost) {
    return "You are currently lost.";
  }
  else {
    return "You are not currently lost.";
  }
});
endjs

// Go to first day scene.
jump day_1
